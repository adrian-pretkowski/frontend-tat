import React from 'react';
import { Container, Header } from 'semantic-ui-react';

export const PrNumbersPage = () => {
	return (
		<Container style={{ marginTop: '7em' }}>
			<Header as='hs' floated='left'>This functionallity is not ready yet...</Header>
		</Container>
	);
};
